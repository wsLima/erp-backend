spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:erp-db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:root}

logging:
  level:
    root: INFO
